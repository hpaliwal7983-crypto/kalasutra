KALASUTRA CLEAN INDEX

Replace ONLY index.html.

This is the clean normal app loader:
- no diagnostic overlay
- no new Function/eval
- no error screen
- current app.tsx stays untouched
- current styles.css stays untouched
- current login animation stays untouched

After committing, wait for Render and reload Safari.
